{
  "name": "agent-action-guardrail",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "nem — a fail-closed Jev-powered guardrail for agent actions.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "tsx --test",
    "check": "npm run build && npm test",
    "cli": "tsx src/cli/main.ts",
    "smoke": "tsx src/cli/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
